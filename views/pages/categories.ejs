<%- include('../template/head') -%>
<%- include('../template/sidebar') -%>
<main class="container-login">
    <div class="content-login bg-login m-login">
        <h2>Nueva Categoría</h2>
        <form class="form" action="/categorias/create" method="POST">
            <div class="input-form">
                <label for="name">Nombre</label>
                <input id="name" type="text" name="name" required />
            </div>
            <button class="btn btn-login btn-click" type="submit">
                Guardar
            </button>
        </form>
        <div class="container-cards">
            <% data.forEach((element, i )=> { %>
            <div class="card-category">
                <div class="card-title-category">
                    <p><%= element.dataValues.name%></p>
                </div>
                <div class="card-info-category">
                    <a class="btn-click color-icon" href="/categorias/editar/<%=element.id%>">
                        <ion-icon name="create-outline"></ion-icon>
                    </a>
                    <a class="btn-click color-icon" href="/categorias/eliminar/<%=element.id%>">
                        <ion-icon name="trash-outline"></ion-icon>
                    </a>
                </div>
            </div>
            <% }); %>
        </div>
    </div>
</main>
<%- include('../template/footer') -%>